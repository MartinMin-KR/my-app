# 월 고정비 정리 앱 — 처음부터 끝까지 누가 무엇을 어떻게 하는가

2026-08-31, 직접 질문하며 정리한 문서.
길을 잃으면 이 문장으로 돌아온다:

> **브라우저가 쪽지를 보내면, app.py의 함수 하나가 실행되고, 그 함수가 답장을 만들어 보낸다.**

---

## 등장인물 소개 (정체 구분)

| 이름 | 정체 | 한 줄 역할 |
|---|---|---|
| 브라우저 | 프로그램 (내 쪽) | **클라이언트**. 요청 보내고, 받은 걸 화면에 그림 |
| uvicorn | 프로그램 (서버 쪽) | 8000번 문 열고 손님 받는 홀 직원 |
| FastAPI (`app`) | 파이썬 안의 물건(장부) | "주소 → 담당 함수" **명단표**를 품고 있음 |
| `index()` 등 | 함수 | 실제로 답을 만드는 주방장들 |
| SQLAlchemy | 라이브러리 (번역기) | 파이썬 주문서를 SQL로 번역해 창고에 전달 |
| SQLite (`app.db`) | 파일 | 창고. 파일 하나가 데이터베이스 전부 |
| Jinja2 | 라이브러리 (포장 담당) | HTML 양식의 빈칸을 받은 값으로 **채우기만** 함 |
| CSS / JS / 이미지 | 파일 (`static/`) | 브라우저 도착 후에 일함: 꾸미기 / 움직임 / 그림 |

- **클라이언트** = 요청하는 쪽. 브라우저뿐 아니라 curl, 앱 등 "요청하는 쪽"이면 전부.
- **서버** = 대기하다가 응답하는 쪽. 먼저 말 걸지 않는다. 화면의 모든 것은 요청해서 받아온 것.

---

## 0막 — 개장 준비 (`uvicorn app:app --reload`)

```
uvicorn 실행
  → 파이썬이 app.py 를 위에서 아래로 한 번 읽음
  → @app.get("/") 같은 "등록 줄"이 실행되며 app 장부에 명단표가 적힘
  → uvicorn 이 그 장부를 들고 8000번 문을 열고 대기 시작
```

완성되는 명단표 (우리 앱은 3줄이 전부):

| 방식 | 주소 | 담당 함수 |
|---|---|---|
| GET | `/` | `index` |
| POST | `/expenses` | `create_expense` |
| GET | `/about` | `about` |

**주의해서 구분할 것**
- 명단표에 적힌 건 "요청들"이 아니라 **규칙(담당자)** 이다. 메뉴판에 적힌 건 주문이 아니라 메뉴다.
- FastAPI는 바깥의 별도 프로그램이 아니다. `app = FastAPI()` 로 **파이썬 안에서 만들어지는 장부**다.
- 명단표는 켤 때 굳는다. 그래서 코드를 고치면 재시작해야 하고, `--reload`가 그 재시작을 자동으로 해준다 (서버만 재시작. **브라우저 새로고침은 직접**).
- 요청이 안 와도 서버는 논다? 아니다 — **기다리는 것 자체가 서버의 일**이다.

---

## 1막 — 주문 (브라우저 → 서버)

주소창의 `http://127.0.0.1:8000/` 의 뜻:

```
http://   127.0.0.1   :8000   /
약속(HTTP)  어느 컴퓨터   몇 번 문   그 안의 어디
           (내 자신)    (포트)
```

엔터를 누르면 브라우저는 ① 그 문에 연결하고 ② **정해진 형식의 텍스트**를 보낸다:

```
GET / HTTP/1.1        ← "/ 를 줘"
Host: 127.0.0.1:8000
```

요청은 마법이 아니라 **텍스트**다. 이 형식의 약속이 HTTP.

서버 쪽 처리 (직접 정리한 문장):

> 브라우저가 서버에 요청을 보낸다. 기다리고 있던 서버는 해당 요청을 받고,
> FastAPI가 갖고 있던 명단표와 대조하여 담당 함수를 실행하고,
> 그 실행 결과를 답장으로 클라이언트에게 보낸다.

- 명단에 없는 주소(`GET /hello`) → 담당자 없음 → **404**
- 방식+주소 **둘 다** 맞아야 한다 (`GET /expenses` 는 매칭 실패)

---

## 2막 — 주방 (`index()` 안, 전부 서버 쪽)

`index()` 는 `def` 로 정의된 평범한 파이썬 함수다. 하는 일 3단계:

### ① 재료 조달 — SQLAlchemy에게 "시키는" 법

```python
with SessionLocal() as db:                              # 창구(세션) 열기
    expenses = db.scalars(
        select(Expense).order_by(Expense.billing_day)   # 주문서 쓰기
    ).all()                                             # 제출 + 결과 받기
```

시킨다 = **파이썬 문법으로 주문서를 써서 창구에 내미는 것**. 그러면:

```
select(Expense).order_by(...)  ── SQLAlchemy 번역 ──▶  SELECT ... FROM expenses ORDER BY billing_day
                                                             │
                                                             ▼
                                        SQLite(app.db) 가 줄들을 찾아 반환
                                                             │
Expense 객체 리스트로 받음  ◀── SQLAlchemy 가 변환 ◀─────────┘
```

SQL 읽는 법: `SELECT * FROM expenses WHERE billing_day = 20`
= "expenses 표에서, 결제일이 20인 **줄**만, 모든 **열**을 가져와"
(WHERE 는 줄 고르기, SELECT 뒤는 열 고르기)

### ② 재료 다듬기 — 계산

```python
total     = 전부 더함
remaining = 결제일이 오늘 이후인 것만 더함
```

### ③ 포장 — Jinja2

**index() 는 양식에 어떤 빈칸이 있는지 이미 알고 있다.** 둘 다 우리가 짝 맞춰 썼기 때문이다 (자동이 아니라 약속):

```
index.html 의 빈칸              index() 가 context 쟁반에 담는 것
{{ title }}              ◀──   "월 고정비 정리"
{% for e in expenses %}  ◀──   창고에서 꺼낸 목록
{{ total }}              ◀──   전체 합계
{{ remaining }}          ◀──   남은 금액
{{ today_day }}          ◀──   오늘 며칠
```

Jinja2 는 **채우는 애** — 그게 전부다:

```html
채우기 전 (templates/index.html)      채운 후 (브라우저가 받는 것)
<h1>{{ title }}</h1>          →       <h1>월 고정비 정리</h1>
<strong>{{ total }}원</strong> →       <strong>586,900원</strong>
```

Jinja2 가 **안 하는 일**: 창고에 안 간다(재료는 index()가 갖다줌), 계산 안 한다, 브라우저로 안 간다. Jinja2 가 일을 시작할 땐 이미 모든 값이 완성돼 있다.

**함정**: 쟁반의 키 이름과 빈칸 이름이 안 맞으면? 에러 없이 **조용히 빈칸**.
약속은 우리 머릿속에만 있고 컴퓨터는 약속을 모른다. "합계가 안 보여요" → 양쪽 이름부터 비교.

완성 HTML 은 `index()` 가 return → FastAPI → uvicorn 이 발송. HTML 을 보내는 건 **서버**다.

---

## 3막 — 화면 (브라우저 쪽, 서버는 손 뗌)

브라우저가 HTML(텍스트)을 받은 뒤:

```
① 읽기      위에서 아래로 읽으며 구조 파악
② 심부름    <link>, <img>, <script> 줄을 만나면 추가 GET 요청 발사
③ 그리기    HTML 구조 + CSS 규칙을 합쳐 화면에 그림
④ 대본 실행  JS 를 실행 (오늘 날짜를 빈 <span> 에 써넣음)
```

**②를 어떻게 발견하나** — 발견이 아니라 우리가 적어둔 걸 읽는 것이다:

```html
<link rel="stylesheet" href=".../static/css/style.css">   "이 CSS 가져와 입혀라"
<img src=".../static/img/coin.svg">                       "이 그림 가져와 표시해라"
<script src=".../static/js/main.js"></script>             "이 JS 가져와 실행해라"
```

이 태그들 자체가 "외부 파일 필요"라는 HTML 문법. **안 적으면 안 가져온다** (CSS 주석 처리 실험이 그 증명 — 파일은 있었는데 가져오라는 줄이 없으니 날것 화면).

- `static/` = 양식 채우기 없이 **그대로** 내주는 구역 (그래서 static, "정적")
- `coin.svg` = 동전 아이콘. SVG 는 "원 그리고 ₩ 써라" 식의 **글자로 그림을 설명하는 형식**. 연습용 장식이라 지워도 앱은 멀쩡
- 페이지 하나에 터미널 로그가 여러 줄인 이유 = 이 추가 요청들

**서버/브라우저 경계선**

```
서버에서 (파이썬 + Jinja2)          브라우저에서 (CSS + JS)
표 내용, 합계, 빈칸 채우기          꾸미기, 날짜 채우기
= "무엇을 보여줄지"                = "어떻게 보이고 움직일지"
```

- 페이지 소스 보기 = 서버가 보낸 원본 (`<span id="today"></span>` 빈칸)
- 검사 = JS 실행 후 현재 상태 (날짜 들어 있음)

화면에서 누가 뭘 만들었나:
- "월세 500,000원" 줄 → **Jinja2** (서버에서 채운 것)
- 테두리선, 파란 [추가] 버튼 → **CSS**
- "오늘: 2026-08-31" 날짜 → **JS**

---

## 다른 방향의 흐름 — [추가] 버튼 (POST)

지금까지는 창고 → 화면 (읽기). [추가] 는 유일하게 반대 방향이다:

```
① 폼에 입력 후 [추가] 클릭
② 브라우저 → 서버: POST /expenses (name=토스프라임&amount=5900&billing_day=1)
   ("줘"가 아니라 "보낸다". 입력값이 요청에 실려 감)
③ 명단표 대조 → create_expense() 실행
   Form(...) 이 입력값을 받아 → Expense 객체로 만들어 → db.add() → db.commit()
④ 서버 → 브라우저: "303, / 로 가" (HTML 대신 안내판)
⑤ 브라우저가 알아서 GET / 를 다시 보냄 → 2막·3막 반복 → 새 줄 포함된 표
```

- `<input name="name">` 의 name 과 `Form(...)` 파라미터 이름이 **짝** (Jinja2 빈칸 약속과 같은 원리)
- `add` = 장바구니 담기, `commit` = 결제. **commit 없으면 저장 안 됨**
- 303 으로 돌려보내는 이유: 저장 직후 새로고침해도 폼이 또 제출되지 않게

---

## 데이터 3형제 (표가 바뀔 때)

```
models.py (설계도)  ── Alembic (공사 일지) ──▶  app.db (건물)
        └────────── SQLAlchemy (번역기) ──────────┘
```

- `models.py`: 클래스 = 표, 속성 = 열, 객체 = 한 줄. 설계도일 뿐 **실행해도 표는 안 생긴다**
- Alembic 두 명령: `revision --autogenerate` = 설계도와 건물을 **비교해서 차이를 기록**,
  `upgrade head` = 기록을 건물에 적용 (기존 데이터 보존)
- Alembic 은 저절로 감시하지 않는다. **명령을 쳤을 때만** 비교한다
- 설계도만 바꾸고 안 돌리면? SQLAlchemy 가 없는 열을 달라고 해서 **500 에러** (조용히 넘어가지 않는다)
- 우리 일지 2장: ① expenses 표 생성 ② memo 열 추가

---

## 반복해서 나온 패턴 — "먼저 모아두고, 한 번에 확정"

| 도구 | 모아두기 | 확정 |
|---|---|---|
| DB | `db.add()` (장바구니) | `db.commit()` (결제) |
| git | `git add .` (대기줄) | `git commit` (스냅샷) |
| Alembic | `revision` (일지 작성) | `upgrade` (적용) |

도구는 달라도 발상이 같다. 새 도구를 만나면 이 패턴부터 의심해보기.

## 틀렸을 때 반응 비교 (디버깅 감각)

| 상황 | 반응 |
|---|---|
| 명단에 없는 주소 | **404** — 시끄러움 |
| 설계도 ≠ 건물 (Alembic 안 돌림) | **500** — 시끄러움 |
| context 키 ≠ 빈칸 이름 | **조용히 빈칸** — 제일 위험. 이름 짝부터 확인 |

---

## 파일 지도

```
my-app/
├── app.py            사령탑. 명단표 등록 3건 + 주방장 함수 3개
├── database.py       창고 연결 (engine, SessionLocal, Base)
├── models.py         표 설계도 (class Expense)
├── alembic/versions/ 공사 일지 2장
├── templates/index.html   빈칸 뚫린 양식
├── static/           그대로 내주는 구역 (css / js / img)
├── .env              창고 주소 (git 제외)
├── app.db            창고 그 자체 (git 제외)
└── requirements.txt  패키지 목록
```

읽는 순서는 항상 `app.py` 부터 — 모든 주소가 거기 모여 있고, 나머지는 전부 app.py 가 불러다 쓰는 부품이다.
